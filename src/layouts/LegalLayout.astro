---
/**
 * Legal Pages Layout
 *
 * Wrapper layout for legal pages (Privacy Policy, Terms & Conditions, Cookies Policy).
 * Uses LegalBaseLayout which imports tailwind.legal.css instead of styles.scss.
 *
 * This separation ensures legal CSS (Typography plugin) doesn't load on the homepage.
 */
import LegalBaseLayout from './LegalBaseLayout.astro';

// Pass through all props to LegalBaseLayout
const props = Astro.props;
---

<LegalBaseLayout {...props}>
  <slot />
</LegalBaseLayout>
