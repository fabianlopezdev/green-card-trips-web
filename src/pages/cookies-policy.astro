---
import CookiesPolicyPage from "@modules/cookiesPolicy";
import Layout from "Layout.astro";
import defaultTemplateConfig from "utils/config";
---

<Layout
  {...defaultTemplateConfig}
  seo={defaultTemplateConfig.cookiesPolicy?.en?.seo}
>
  <CookiesPolicyPage client:idle config={defaultTemplateConfig} />
</Layout>
